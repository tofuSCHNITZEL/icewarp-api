"""Auto-generated by scripts/generate_client.py - do not edit by hand.

Typed wrappers for endpoints tagged 'Certificate related methods' in the IceWarp Maintenance API.
"""

from __future__ import annotations

from typing import Any, Optional, TYPE_CHECKING

if TYPE_CHECKING:
    from ..client import IceWarpClient


class CertificateMethods:
    """Typed wrappers for endpoints tagged 'Certificate related methods'."""

    def __init__(self, client: "IceWarpClient") -> None:
        self._client = client

    def add_server_certificate(self, *, ipaddress: Optional[Any] = None, certificate: Optional[str] = None, setasdefault: Optional[str] = None) -> Any:
        """Add new server certificate (uploaded)

        Add new server certificate (uploaded)

        **Parameters**

        - IPAddress: IP Address of the server certificate
        - Certificate: Certificate data (base64 encoded data string)
        - SetAsDefault: Tells the server if set this certificate as new default certificate (obsolete)


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /AddServerCertificate``

        Args:
            ipaddress: Maps to the ``ipaddress`` request parameter.
            certificate: Maps to the ``certificate`` request parameter.
            setasdefault: Maps to the ``setasdefault`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "ipaddress": ipaddress,
            "certificate": certificate,
            "setasdefault": setasdefault,
        }
        return self._client.call("AddServerCertificate", **params)

    def create_server_certificate(self, *, certificate: Optional[Any] = None, setasdefault: Optional[str] = None) -> Any:
        """Create new CSR / Server certificate

        Create new CSR / Server certificate

        **Parameters**

        - Certificate: Certificate request data (see TServerCertificateRequest)
        - SetAsDefault: Tells the server if set this certificate as new default certificate  (obsolete)


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /CreateServerCertificate``

        Args:
            certificate: Maps to the ``certificate`` request parameter.
            setasdefault: Maps to the ``setasdefault`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "certificate": certificate,
            "setasdefault": setasdefault,
        }
        return self._client.call("CreateServerCertificate", **params)

    def delete_server_certificate(self, *, id: Optional[str] = None) -> Any:
        """Deletes existing server certificate

        Deletes existing server certificate

        **Parameters**

        - ID: Certificate ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /DeleteServerCertificate``

        Args:
            id: Maps to the ``id`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "id": id,
        }
        return self._client.call("DeleteServerCertificate", **params)

    def edit_server_certificate(self, *, id: Optional[str] = None, ipaddress: Optional[Any] = None, certificate: Optional[str] = None, setasdefault: Optional[str] = None) -> Any:
        """Edits existing server certificate

        Edits existing server certificate

        **Parameters**

        - ID: Certificate ID
        - IPAddress: New IP Address
        - Certificate: New certificate data (base64 encoded data string)
        - SetAsDefault: Tells the server if set this certificate as new default certificate (obsolete)


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /EditServerCertificate``

        Args:
            id: Maps to the ``id`` request parameter.
            ipaddress: Maps to the ``ipaddress`` request parameter.
            certificate: Maps to the ``certificate`` request parameter.
            setasdefault: Maps to the ``setasdefault`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "id": id,
            "ipaddress": ipaddress,
            "certificate": certificate,
            "setasdefault": setasdefault,
        }
        return self._client.call("EditServerCertificate", **params)

    def export_server_certificate(self, *, id: Optional[str] = None) -> Any:
        """Exports existing server certificate

        Exports existing server certificate

        **Parameters**

        - ID: Certificate ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /ExportServerCertificate``

        Args:
            id: Maps to the ``id`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "id": id,
        }
        return self._client.call("ExportServerCertificate", **params)

    def get_server_certificate_list(self) -> Any:
        """Rertrieves the list of server certificates

        Rertrieves the list of server certificates

        **Parameters**



        **Returns**

        List of server certificates ( see TServerCertificateList )

        Endpoint: ``POST /GetServerCertificateList``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetServerCertificateList")

    def reissue_server_certificate(self, *, id: Optional[str] = None, certificate: Optional[Any] = None, reuse: Optional[str] = None) -> Any:
        """Reissues existing server certificate

        Reissues existing server certificate

        **Parameters**

        - ID: Certificate ID
        - Certificate: Certificate data
        - ReUse: Specifies whether to regenerate certificate private key(0) or use the old one(1)


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /ReissueServerCertificate``

        Args:
            id: Maps to the ``id`` request parameter.
            certificate: Maps to the ``certificate`` request parameter.
            reuse: Maps to the ``reuse`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "id": id,
            "certificate": certificate,
            "reuse": reuse,
        }
        return self._client.call("ReissueServerCertificate", **params)

    def set_default_server_certificate(self, *, id: Optional[str] = None) -> Any:
        """Sets the existing server certificate as default

        Sets the existing server certificate as default

        **Parameters**

        - ID: Certificate ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /SetDefaultServerCertificate``

        Args:
            id: Maps to the ``id`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "id": id,
        }
        return self._client.call("SetDefaultServerCertificate", **params)
