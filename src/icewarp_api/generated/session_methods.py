"""Auto-generated by scripts/generate_client.py - do not edit by hand.

Typed wrappers for endpoints tagged 'Session related methods' in the IceWarp Maintenance API.
"""

from __future__ import annotations

from typing import Any, Optional, TYPE_CHECKING

if TYPE_CHECKING:
    from ..client import IceWarpClient


class SessionMethods:
    """Typed wrappers for endpoints tagged 'Session related methods'."""

    def __init__(self, client: "IceWarpClient") -> None:
        self._client = client

    def authenticate(self, *, authtype: Optional[str] = None, email: Optional[str] = None, password: Optional[str] = None, digest: Optional[str] = None, persistentlogin: Optional[str] = None, totpcode: Optional[str] = None) -> Any:
        """Authenticates user in IceWarp server and creates his session

        Authenticates user in IceWarp server and creates his session

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType
        - Email: Account username or email address - depends on server login policy
        - Password: Account plain text password
        - Digest: Account encrypted password
        - PersistentLogin: Specifies the session expiration - 1 day or 24 days


        **Returns**

        Session ID of authenticated user

        Endpoint: ``POST /Authenticate``

        Args:
            authtype: Maps to the ``authtype`` request parameter.
            email: Maps to the ``email`` request parameter.
            password: Maps to the ``password`` request parameter.
            digest: Maps to the ``digest`` request parameter.
            persistentlogin: Maps to the ``persistentlogin`` request parameter.
            totpcode: Maps to the ``totpcode`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authtype": authtype,
            "email": email,
            "password": password,
            "digest": digest,
            "persistentlogin": persistentlogin,
            "totpcode": totpcode,
        }
        return self._client.call("Authenticate", **params)

    def authenticate_gateway(self, *, hash: Optional[str] = None) -> Any:
        """Authenticates gateway account ( account that does not exist on current server , but hash some quarantine here )

        Authenticates gateway account ( account that does not exist on current server , but hash some quarantine here )

        **Parameters**

        - Hash: Hash received in gateway authentication email


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /AuthenticateGateway``

        Args:
            hash: Maps to the ``hash`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "hash": hash,
        }
        return self._client.call("AuthenticateGateway", **params)

    def authenticate_jwt(self, *, token: Optional[str] = None) -> Any:
        """Creates user session by provided JWT token

        Creates user session by provided JWT token

        **Parameters**

        - Token: JWT token can be obtained with GetJWTToken method or OAuth token can be used


        **Returns**

        Session ID of authenticated user in sid and authentication token in result

        Endpoint: ``POST /AuthenticateJWT``

        Args:
            token: Maps to the ``token`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "token": token,
        }
        return self._client.call("AuthenticateJWT", **params)

    def authenticate_sso(self, *, ssotoken: Optional[str] = None, host: Optional[str] = None) -> Any:
        """Authenticates using authentication SSO token

        Authenticates using authentication SSO token

        **Parameters**

        - SSOToken: Authentication sso token
        - Host: Host name


        **Returns**

        1 on success, 0 when there is some failiure + SID

        Endpoint: ``POST /AuthenticateSSO``

        Args:
            ssotoken: Maps to the ``ssotoken`` request parameter.
            host: Maps to the ``host`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "ssotoken": ssotoken,
            "host": host,
        }
        return self._client.call("AuthenticateSSO", **params)

    def authenticate_team_chat_api(self, *, externaldeviceid: Optional[str] = None) -> Any:
        """Returns the authentication token for TeamChat for the currently logged in user

        Returns the authentication token for TeamChat for the currently logged in user

        **Parameters**

        - ExternalDeviceID: External deviceid for push notifications (the as TC API login "external_deviceid")


        **Returns**

        TeamChat token

        Endpoint: ``POST /AuthenticateTeamChatApi``

        Args:
            externaldeviceid: Maps to the ``externaldeviceid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "externaldeviceid": externaldeviceid,
        }
        return self._client.call("AuthenticateTeamChatApi", **params)

    def authenticate_token(self, *, token: Optional[str] = None) -> Any:
        """Authenticates using authentication token

        Authenticates using authentication token

        **Parameters**

        - Token: Authentication token


        **Returns**

        1 on success, 0 when there is some failiure + SID

        Endpoint: ``POST /AuthenticateToken``

        Args:
            token: Maps to the ``token`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "token": token,
        }
        return self._client.call("AuthenticateToken", **params)

    def authenticate_web_client(self, *, webclientsessionid: Optional[str] = None) -> Any:
        """Authenticates using weblclient session id

        Authenticates using weblclient session id

        **Parameters**

        - WebClientSessionID: WebClient Session ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /AuthenticateWebClient``

        Args:
            webclientsessionid: Maps to the ``webclientsessionid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "webclientsessionid": webclientsessionid,
        }
        return self._client.call("AuthenticateWebClient", **params)

    def get_account_chat_history(self, *, accountemail: Optional[str] = None, withemail: Optional[str] = None, from_: Optional[str] = None, to_: Optional[str] = None, max: Optional[str] = None) -> Any:
        """Get user IM roster

        Get user IM roster

        **Parameters**

        - accountemail: email address of IceWarp account existing on server
        - withemail: roster contact email
        - from: if from is missing: infinity
        - to: if to is missing: current time


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /GetAccountChatHistory``

        Args:
            accountemail: Maps to the ``accountemail`` request parameter.
            withemail: Maps to the ``withemail`` request parameter.
            from_: Maps to the ``from`` request parameter.
            to_: Maps to the ``to_`` request parameter.
            max: Maps to the ``max`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "accountemail": accountemail,
            "withemail": withemail,
            "from": from_,
            "to_": to_,
            "max": max,
        }
        return self._client.call("GetAccountChatHistory", **params)

    def get_account_chat_roster(self, *, accountemail: Optional[str] = None) -> Any:
        """Get user IM roster

        Get user IM roster

        **Parameters**

        - accountemail: email address of IceWarp account existing on server


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /GetAccountChatRoster``

        Args:
            accountemail: Maps to the ``accountemail`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "accountemail": accountemail,
        }
        return self._client.call("GetAccountChatRoster", **params)

    def get_auth_challenge(self, *, authtype: Optional[str] = None) -> Any:
        """Get the challenge string for RSA authentication type

        Get the challenge string for RSA authentication type

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType


        **Returns**

        Authentication challegenge string ( currently RSA public key ) see TAuthChallenge

        Endpoint: ``POST /GetAuthChallenge``

        Args:
            authtype: Maps to the ``authtype`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authtype": authtype,
        }
        return self._client.call("GetAuthChallenge", **params)

    def get_auth_token(self, *, authtype: Optional[str] = None, email: Optional[str] = None, password: Optional[str] = None, digest: Optional[str] = None, persistentlogin: Optional[str] = None, totpcode: Optional[str] = None) -> Any:
        """Authenticates and returns authentication token that can be used for both webadmin and webclient

        Authenticates and returns authentication token that can be used for both webadmin and webclient

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType
        - Email: Account username or email address - depends on server login policy
        - Password: Account plain text password
        - Digest: Account encrypted password
        - PersistentLogin: Specifies the session expiration - 1 day or 24 days


        **Returns**

        Authentication token

        Endpoint: ``POST /GetAuthToken``

        Args:
            authtype: Maps to the ``authtype`` request parameter.
            email: Maps to the ``email`` request parameter.
            password: Maps to the ``password`` request parameter.
            digest: Maps to the ``digest`` request parameter.
            persistentlogin: Maps to the ``persistentlogin`` request parameter.
            totpcode: Maps to the ``totpcode`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authtype": authtype,
            "email": email,
            "password": password,
            "digest": digest,
            "persistentlogin": persistentlogin,
            "totpcode": totpcode,
        }
        return self._client.call("GetAuthToken", **params)

    def get_avatar(self, *, email: Optional[str] = None) -> Any:
        """Authenticates and returns authentication token that can be used for both webadmin and webclient

        Authenticates and returns authentication token that can be used for both webadmin and webclient

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType


        **Returns**

        Authentication token

        Endpoint: ``POST /GetAvatar``

        Args:
            email: Maps to the ``email`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "email": email,
        }
        return self._client.call("GetAvatar", **params)

    def get_avatar_token(self, *, email: Optional[str] = None) -> Any:
        """Authenticates and returns authentication token that can be used for both webadmin and webclient

        Authenticates and returns authentication token that can be used for both webadmin and webclient

        **Parameters**

        - Email: Email


        **Returns**

        Authentication token

        Endpoint: ``POST /GetAvatarToken``

        Args:
            email: Maps to the ``email`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "email": email,
        }
        return self._client.call("GetAvatarToken", **params)

    def get_jwt_token(self, *, authtype: Optional[str] = None, email: Optional[str] = None, password: Optional[str] = None, digest: Optional[str] = None, totpcode: Optional[str] = None) -> Any:
        """Authenticates user and creates JWT token for him

        Authenticates user and creates JWT token for him

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType
        - Email: Account username or email address - depends on server login policy
        - Password: Account plain text password
        - Digest: Account encrypted password


        **Returns**

        JWT token which can be used for creating session by calling AuthenticateJWT method

        Endpoint: ``POST /GetJWTToken``

        Args:
            authtype: Maps to the ``authtype`` request parameter.
            email: Maps to the ``email`` request parameter.
            password: Maps to the ``password`` request parameter.
            digest: Maps to the ``digest`` request parameter.
            totpcode: Maps to the ``totpcode`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authtype": authtype,
            "email": email,
            "password": password,
            "digest": digest,
            "totpcode": totpcode,
        }
        return self._client.call("GetJWTToken", **params)

    def get_session_info(self) -> Any:
        """Info about active session , see TAPISessionInfo for more details

        Info about active session , see TAPISessionInfo for more details

        **Parameters**



        **Returns**

        Info about active session , see TAPISessionInfo for more details

        Endpoint: ``POST /GetSessionInfo``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetSessionInfo")

    def impersonate_web_client(self, *, email: Optional[str] = None) -> Any:
        """Impersonate as another person and get its webclient session id
  Administrator can impersonate anyone
  Domain administrator can impersonate accounts from his domain

        Impersonate as another person and get its webclient session id
          Administrator can impersonate anyone
          Domain administrator can impersonate accounts from his domain

        **Parameters**

        - Email: Email of gateway account


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /ImpersonateWebClient``

        Args:
            email: Maps to the ``email`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "email": email,
        }
        return self._client.call("ImpersonateWebClient", **params)

    def login_jwt_token(self, *, authtype: Optional[str] = None, email: Optional[str] = None, password: Optional[str] = None, digest: Optional[str] = None, clientident: Optional[str] = None, totpcode: Optional[str] = None) -> Any:
        """Authenticates user and creates JWT token for him

        Authenticates user and creates JWT token for him

        **Parameters**

        - AuthType: Type of authentication ( plain, rsa, ... ) - see TAuthType
        - Email: Account username or email address - depends on server login policy
        - Password: Account plain text password
        - Digest: Account encrypted password
        - ClientIdent: Custom client identification


        **Returns**

        JWT token which can be used for creating session by calling AuthenticateJWT method

        Endpoint: ``POST /LoginJWTToken``

        Args:
            authtype: Maps to the ``authtype`` request parameter.
            email: Maps to the ``email`` request parameter.
            password: Maps to the ``password`` request parameter.
            digest: Maps to the ``digest`` request parameter.
            clientident: Maps to the ``clientident`` request parameter.
            totpcode: Maps to the ``totpcode`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authtype": authtype,
            "email": email,
            "password": password,
            "digest": digest,
            "clientident": clientident,
            "totpcode": totpcode,
        }
        return self._client.call("LoginJWTToken", **params)

    def logout(self) -> Any:
        """Logs the user out and destroy his session

        Logs the user out and destroy his session

        **Parameters**



        **Returns**

        Endpoint: ``POST /Logout``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("Logout")

    def logout_jwt_token(self, *, token: Optional[str] = None) -> Any:
        """Logouts refresh and access token

        Logouts refresh and access token

        **Parameters**



        **Returns**

        Endpoint: ``POST /LogoutJWTToken``

        Args:
            token: Maps to the ``token`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "token": token,
        }
        return self._client.call("LogoutJWTToken", **params)

    def make_jwt_token_login(self, *, clientident: Optional[str] = None) -> Any:
        """This method creates access and refresh token for user with session.
 It creates new JWT login into login table.

        This method creates access and refresh token for user with session.
         It creates new JWT login into login table.

        **Parameters**



        **Returns**

        Endpoint: ``POST /MakeJWTTokenLogin``

        Args:
            clientident: Maps to the ``clientident`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "clientident": clientident,
        }
        return self._client.call("MakeJWTTokenLogin", **params)

    def refresh_jwt_token(self, *, refreshtoken: Optional[str] = None) -> Any:
        """Creates access token for valid refreshtoken

        Creates access token for valid refreshtoken

        **Parameters**



        **Returns**

        Endpoint: ``POST /RefreshJWTToken``

        Args:
            refreshtoken: Maps to the ``refreshtoken`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "refreshtoken": refreshtoken,
        }
        return self._client.call("RefreshJWTToken", **params)

    def reset2_factor(self, *, email: Optional[str] = None) -> Any:
        """Resets 2 factor authorization for account given in Email parameter

        Resets 2 factor authorization for account given in Email parameter

        **Parameters**

        - Email: Full email address of account whose 2 factor setup will be reset


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /Reset2Factor``

        Args:
            email: Maps to the ``email`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "email": email,
        }
        return self._client.call("Reset2Factor", **params)

    def reset_my2_factor(self, *, code: Optional[str] = None) -> Any:
        """Resets 2 factor authorization for authenticated user

        Resets 2 factor authorization for authenticated user

        **Parameters**

        - Code: Two-factor code from existing token generator


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /ResetMy2Factor``

        Args:
            code: Maps to the ``code`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "code": code,
        }
        return self._client.call("ResetMy2Factor", **params)

    def send_gateway_authentication(self, *, email: Optional[str] = None, captcharequest: Optional[Any] = None) -> Any:
        """Sends the authentication link ( with Hash ) to the email

        Sends the authentication link ( with Hash ) to the email

        **Parameters**

        - Email: Email of gateway account


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /SendGatewayAuthentication``

        Args:
            email: Maps to the ``email`` request parameter.
            captcharequest: Maps to the ``captcharequest`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "email": email,
            "captcharequest": captcharequest,
        }
        return self._client.call("SendGatewayAuthentication", **params)
