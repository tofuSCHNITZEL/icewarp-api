"""Auto-generated by scripts/generate_client.py - do not edit by hand.

Typed wrappers for endpoints tagged 'OAuth related methods' in the IceWarp Maintenance API.
"""

from __future__ import annotations

from typing import Any, Optional, TYPE_CHECKING

if TYPE_CHECKING:
    from ..client import IceWarpClient


class OauthMethods:
    """Typed wrappers for endpoints tagged 'OAuth related methods'."""

    def __init__(self, client: "IceWarpClient") -> None:
        self._client = client

    def add_oauth_client(self, *, client: Optional[Any] = None) -> Any:
        """Register new OAuth client

        Register new OAuth client

        **Parameters**



        **Returns**

        New client id and secret

        Endpoint: ``POST /AddOauthClient``

        Args:
            client: Maps to the ``client`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "client": client,
        }
        return self._client.call("AddOauthClient", **params)

    def add_oauth_client_with_id(self, *, client: Optional[Any] = None) -> Any:
        """Represents function <span class="dcword">TAPISession.AddOauthClientWithId</span>

        Represents function <span class="dcword">TAPISession.AddOauthClientWithId</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /AddOauthClientWithId``

        Args:
            client: Maps to the ``client`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "client": client,
        }
        return self._client.call("AddOauthClientWithId", **params)

    def authorized_to_im_query(self, *, accountemail: Optional[str] = None) -> Any:
        """Represents function <span class="dcword">TAPISession.AuthorizedToIMQuery</span>

        Represents function <span class="dcword">TAPISession.AuthorizedToIMQuery</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /AuthorizedToIMQuery``

        Args:
            accountemail: Maps to the ``accountemail`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "accountemail": accountemail,
        }
        return self._client.call("AuthorizedToIMQuery", **params)

    def create_oauth_signed_token(self, *, payload: Optional[str] = None) -> Any:
        """Not published

        Not published

        **Parameters**



        **Returns**

        Endpoint: ``POST /CreateOAuthSignedToken``

        Args:
            payload: Maps to the ``payload`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "payload": payload,
        }
        return self._client.call("CreateOAuthSignedToken", **params)

    def edit_oauth_client(self, *, client: Optional[Any] = None) -> Any:
        """Allows edit informations about registered OAuth client

        Allows edit informations about registered OAuth client

        **Parameters**



        **Returns**

        TAPIOauthNewClientResult. In case that auth type is not changed or new type does not require secret than empty secret is returned.
         When type is changed to standard OAuth application than new secret is returned.

        Endpoint: ``POST /EditOauthClient``

        Args:
            client: Maps to the ``client`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "client": client,
        }
        return self._client.call("EditOauthClient", **params)

    def get_actual_email(self) -> Any:
        """Represents function <span class="dcword">TAPISession.GetActualEmail</span>

        Represents function <span class="dcword">TAPISession.GetActualEmail</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /GetActualEmail``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetActualEmail")

    def get_dns_zone_file(self, *, domain: Optional[str] = None) -> Any:
        """Returns DNS zone file as base64 encoded string

        Returns DNS zone file as base64 encoded string

        **Parameters**



        **Returns**

        Ansistring

        Endpoint: ``POST /GetDNSZoneFile``

        Args:
            domain: Maps to the ``domain`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "domain": domain,
        }
        return self._client.call("GetDNSZoneFile", **params)

    def get_oauth_access_code(self, *, accesscodevalue: Optional[str] = None) -> Any:
        """It gets informations about access code.

        It gets informations about access code.

        **Parameters**



        **Returns**

        TAPIOauthAccessCode

        Endpoint: ``POST /GetOauthAccessCode``

        Args:
            accesscodevalue: Maps to the ``accesscodevalue`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "accesscodevalue": accesscodevalue,
        }
        return self._client.call("GetOauthAccessCode", **params)

    def get_oauth_authorization_request(self, *, requestid: Optional[str] = None) -> Any:
        """It gets informations about authorization request.

        It gets informations about authorization request.

        **Parameters**



        **Returns**

        TAPIOauthRequestInfo

        Endpoint: ``POST /GetOauthAuthorizationRequest``

        Args:
            requestid: Maps to the ``requestid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "requestid": requestid,
        }
        return self._client.call("GetOauthAuthorizationRequest", **params)

    def get_oauth_authorization_uri(self) -> Any:
        """Returns uri where client will be authorized for oauth by user.

        Returns uri where client will be authorized for oauth by user.

        **Parameters**



        **Returns**

        AnsiString

        Endpoint: ``POST /GetOauthAuthorizationUri``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetOauthAuthorizationUri")

    def get_oauth_authorizations(self) -> Any:
        """It gets informations about users granted authorization.

        It gets informations about users granted authorization.

        **Parameters**



        **Returns**

        List of users authorizations

        Endpoint: ``POST /GetOauthAuthorizations``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetOauthAuthorizations")

    def get_oauth_client_info(self, *, clientid: Optional[str] = None) -> Any:
        """Returns informations about registered OAuth client

        Returns informations about registered OAuth client

        **Parameters**



        **Returns**

        TAPIOauthClientInfo

        Endpoint: ``POST /GetOauthClientInfo``

        Args:
            clientid: Maps to the ``clientid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "clientid": clientid,
        }
        return self._client.call("GetOauthClientInfo", **params)

    def get_oauth_client_registration(self, *, clientid: Optional[str] = None) -> Any:
        """Represents function <span class="dcword">TAPISession.GetOauthClientRegistration</span>

        Represents function <span class="dcword">TAPISession.GetOauthClientRegistration</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /GetOauthClientRegistration``

        Args:
            clientid: Maps to the ``clientid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "clientid": clientid,
        }
        return self._client.call("GetOauthClientRegistration", **params)

    def get_oauth_clients(self) -> Any:
        """Returns list of registered OAuth clients

        Returns list of registered OAuth clients

        **Parameters**



        **Returns**

        List of OAuht clients

        Endpoint: ``POST /GetOauthClients``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetOauthClients")

    def get_oauth_refresh_token(self, *, refreshtokenvalue: Optional[str] = None) -> Any:
        """It gets informations about refresh token.

        It gets informations about refresh token.

        **Parameters**



        **Returns**

        TAPIOauthTokenInfo

        Endpoint: ``POST /GetOauthRefreshToken``

        Args:
            refreshtokenvalue: Maps to the ``refreshtokenvalue`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "refreshtokenvalue": refreshtokenvalue,
        }
        return self._client.call("GetOauthRefreshToken", **params)

    def get_oauth_scopes(self) -> Any:
        """Returns OAuth scopes.

        Returns OAuth scopes.

        **Parameters**



        **Returns**

        List of scopes

        Endpoint: ``POST /GetOauthScopes``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetOauthScopes")

    def get_password(self) -> Any:
        """Represents function <span class="dcword">TAPISession.GetPassword</span>

        Represents function <span class="dcword">TAPISession.GetPassword</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /GetPassword``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetPassword")

    def get_restrict_ip_list(self) -> Any:
        """Represents function <span class="dcword">TAPISession.GetRestrictIPList</span>

        Represents function <span class="dcword">TAPISession.GetRestrictIPList</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /GetRestrictIPList``

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        return self._client.call("GetRestrictIPList")

    def grant_oauth_authorization(self, *, requestid: Optional[str] = None, description: Optional[str] = None) -> Any:
        """Logged user grants authorization to authorization request.

        Logged user grants authorization to authorization request.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /GrantOauthAuthorization``

        Args:
            requestid: Maps to the ``requestid`` request parameter.
            description: Maps to the ``description`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "requestid": requestid,
            "description": description,
        }
        return self._client.call("GrantOauthAuthorization", **params)

    def oauth_authorization_exists(self, *, requestid: Optional[str] = None) -> Any:
        """It checks if logged user already granted authorization to client in the past.
 If authorization is granted than authorization request is paired with already existing grant.

        It checks if logged user already granted authorization to client in the past.
         If authorization is granted than authorization request is paired with already existing grant.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /OauthAuthorizationExists``

        Args:
            requestid: Maps to the ``requestid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "requestid": requestid,
        }
        return self._client.call("OauthAuthorizationExists", **params)

    def oauth_verify_secret(self, *, clientid: Optional[str] = None, secret: Optional[str] = None) -> Any:
        """Returns informations about registered OAuth client only if matches secret

        Returns informations about registered OAuth client only if matches secret

        **Parameters**



        **Returns**

        TAPIOauthClientInfo

        Endpoint: ``POST /OauthVerifySecret``

        Args:
            clientid: Maps to the ``clientid`` request parameter.
            secret: Maps to the ``secret`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "clientid": clientid,
            "secret": secret,
        }
        return self._client.call("OauthVerifySecret", **params)

    def remove_oauth_authorization(self, *, authorizationid: Optional[int] = None) -> Any:
        """It removes authorization.

        It removes authorization.

        **Parameters**



        **Returns**

        True on success

        Endpoint: ``POST /RemoveOauthAuthorization``

        Args:
            authorizationid: Maps to the ``authorizationid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "authorizationid": authorizationid,
        }
        return self._client.call("RemoveOauthAuthorization", **params)

    def remove_oauth_client(self, *, clientid: Optional[str] = None) -> Any:
        """Removes OAuth client with all authorizations and tokens

        Removes OAuth client with all authorizations and tokens

        **Parameters**



        **Returns**

        True on success

        Endpoint: ``POST /RemoveOauthClient``

        Args:
            clientid: Maps to the ``clientid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "clientid": clientid,
        }
        return self._client.call("RemoveOauthClient", **params)

    def replace_oauth_access_token(self, *, refreshtoken: Optional[Any] = None, accesstoken: Optional[Any] = None) -> Any:
        """It replaces access token with new one generated by refresh token. Old access token is deleted even if it is not expired.

        It replaces access token with new one generated by refresh token. Old access token is deleted even if it is not expired.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /ReplaceOauthAccessToken``

        Args:
            refreshtoken: Maps to the ``refreshtoken`` request parameter.
            accesstoken: Maps to the ``accesstoken`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "refreshtoken": refreshtoken,
            "accesstoken": accesstoken,
        }
        return self._client.call("ReplaceOauthAccessToken", **params)

    def save_oauth_access_code(self, *, requestid: Optional[str] = None, accesscode: Optional[Any] = None) -> Any:
        """It stores generated access code to the authorization request.

        It stores generated access code to the authorization request.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /SaveOauthAccessCode``

        Args:
            requestid: Maps to the ``requestid`` request parameter.
            accesscode: Maps to the ``accesscode`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "requestid": requestid,
            "accesscode": accesscode,
        }
        return self._client.call("SaveOauthAccessCode", **params)

    def save_oauth_tokens(self, *, accesscodevalue: Optional[str] = None, refreshtoken: Optional[Any] = None, accesstoken: Optional[Any] = None) -> Any:
        """It finishes authorization sequesnce and stores generated tokens.

        It finishes authorization sequesnce and stores generated tokens.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /SaveOauthTokens``

        Args:
            accesscodevalue: Maps to the ``accesscodevalue`` request parameter.
            refreshtoken: Maps to the ``refreshtoken`` request parameter.
            accesstoken: Maps to the ``accesstoken`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "accesscodevalue": accesscodevalue,
            "refreshtoken": refreshtoken,
            "accesstoken": accesstoken,
        }
        return self._client.call("SaveOauthTokens", **params)

    def set_restrict_ip_list(self, *, restrictiplist: Optional[Any] = None) -> Any:
        """Represents function <span class="dcword">TAPISession.SetRestrictIPList</span>

        Represents function <span class="dcword">TAPISession.SetRestrictIPList</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /SetRestrictIPList``

        Args:
            restrictiplist: Maps to the ``restrictiplist`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "restrictiplist": restrictiplist,
        }
        return self._client.call("SetRestrictIPList", **params)

    def start_oauth_authorization_request(self, *, request: Optional[Any] = None) -> Any:
        """Begins application authorization to access to user data.

        Begins application authorization to access to user data.

        **Parameters**



        **Returns**

        Id of authorization request

        Endpoint: ``POST /StartOauthAuthorizationRequest``

        Args:
            request: Maps to the ``request`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "request": request,
        }
        return self._client.call("StartOauthAuthorizationRequest", **params)

    def verify_certificate_chain(self, *, certificate: Optional[str] = None) -> Any:
        """Checks if base64 encode PEM certificate passed as parameter has all intermediate certificates and is not self-signed

        Checks if base64 encode PEM certificate passed as parameter has all intermediate certificates and is not self-signed

        **Parameters**



        **Returns**

        Verification result code

        Endpoint: ``POST /VerifyCertificateChain``

        Args:
            certificate: Maps to the ``certificate`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "certificate": certificate,
        }
        return self._client.call("VerifyCertificateChain", **params)

    def verify_jwt_token(self, *, token: Optional[str] = None) -> Any:
        """It verifies token signature, validity and expiration.

        It verifies token signature, validity and expiration.

        **Parameters**



        **Returns**

        Boolean

        Endpoint: ``POST /VerifyJWTToken``

        Args:
            token: Maps to the ``token`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "token": token,
        }
        return self._client.call("VerifyJWTToken", **params)

    def verify_oauth_token_signature(self, *, jwttoken: Optional[str] = None) -> Any:
        """Represents function <span class="dcword">TAPISession.VerifyOAuthTokenSignature</span>

        Represents function <span class="dcword">TAPISession.VerifyOAuthTokenSignature</span>

        **Parameters**



        **Returns**

        Endpoint: ``POST /VerifyOAuthTokenSignature``

        Args:
            jwttoken: Maps to the ``jwttoken`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "jwttoken": jwttoken,
        }
        return self._client.call("VerifyOAuthTokenSignature", **params)
