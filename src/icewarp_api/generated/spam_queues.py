"""Auto-generated by scripts/generate_client.py - do not edit by hand.

Typed wrappers for endpoints tagged 'Spam queues related methods' in the IceWarp Maintenance API.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client import IceWarpClient


class SpamQueuesMethods:
    """Typed wrappers for endpoints tagged 'Spam queues related methods'."""

    def __init__(self, client: IceWarpClient) -> None:
        self._client = client

    def add_spam_queue_item(self, *, info: Any | None = None) -> Any:
        """Adds the item to the Spam Queues ( whitelist / blacklist )

        Adds the item to the Spam Queues ( whitelist / blacklist )

        **Parameters**

        - Info: Informations about item to be added


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /AddSpamQueueItem``

        Args:
            info: Maps to the ``info`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "info": info,
        }
        return self._client.call("AddSpamQueueItem", **params)

    def black_list_spam_queue_item(self, *, itemid: str | None = None) -> Any:
        """Blacklist the item in Spam Queue

        Blacklist the item in Spam Queue

        **Parameters**

        - ItemID: Spam Queue item ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /BlackListSpamQueueItem``

        Args:
            itemid: Maps to the ``itemid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "itemid": itemid,
        }
        return self._client.call("BlackListSpamQueueItem", **params)

    def delete_spam_queue_item(self, *, itemid: str | None = None) -> Any:
        """Deletes the item from Spam Queues

        Deletes the item from Spam Queues

        **Parameters**

        - ItemID: Spam Queue item ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /DeleteSpamQueueItem``

        Args:
            itemid: Maps to the ``itemid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "itemid": itemid,
        }
        return self._client.call("DeleteSpamQueueItem", **params)

    def deliver_spam_queue_item(self, *, itemid: str | None = None) -> Any:
        """Delivers the item in Spam Queue ( quarantine )

        Delivers the item in Spam Queue ( quarantine )

        **Parameters**

        - ItemID: Spam Queue item ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /DeliverSpamQueueItem``

        Args:
            itemid: Maps to the ``itemid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "itemid": itemid,
        }
        return self._client.call("DeliverSpamQueueItem", **params)

    def get_spam_queue_info_list(self, *, queuetype: str | None = None, filter: Any | None = None, offset: int | None = None, count: int | None = None) -> Any:
        """Gets the spam queue list

        Gets the spam queue list

        **Parameters**

        - QueueType: Queue type specification ( see TSpamQueueType )
        - Filter: Used to filter values in the list
        - Offset: Specifies offset start of returned items
        - Count: Specifies count of returned items


        **Returns**

        List of QueueType items that matches filter

        Endpoint: ``POST /GetSpamQueueInfoList``

        Args:
            queuetype: Maps to the ``queuetype`` request parameter.
            filter: Maps to the ``filter`` request parameter.
            offset: Maps to the ``offset`` request parameter.
            count: Maps to the ``count`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "queuetype": queuetype,
            "filter": filter,
            "offset": offset,
            "count": count,
        }
        return self._client.call("GetSpamQueueInfoList", **params)

    def get_spam_queue_item_body(self, *, itemid: str | None = None) -> Any:
        """Returns the body of Spam Queue item

        Returns the body of Spam Queue item

        **Parameters**

        - ItemID: Spam Queue item ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /GetSpamQueueItemBody``

        Args:
            itemid: Maps to the ``itemid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "itemid": itemid,
        }
        return self._client.call("GetSpamQueueItemBody", **params)

    def white_list_spam_queue_item(self, *, itemid: str | None = None) -> Any:
        """Whitelist the item in Spam Queue

        Whitelist the item in Spam Queue

        **Parameters**

        - ItemID: Spam Queue item ID


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /WhiteListSpamQueueItem``

        Args:
            itemid: Maps to the ``itemid`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "itemid": itemid,
        }
        return self._client.call("WhiteListSpamQueueItem", **params)
