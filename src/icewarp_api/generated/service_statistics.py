"""Auto-generated by scripts/generate_client.py - do not edit by hand.

Typed wrappers for endpoints tagged 'Service &amp; statistics related methods' in the IceWarp Maintenance API.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client import IceWarpClient


class ServiceStatisticsMethods:
    """Typed wrappers for endpoints tagged 'Service &amp; statistics related methods'."""

    def __init__(self, client: IceWarpClient) -> None:
        self._client = client

    def get_service_statistics(self, *, stype: str | None = None) -> Any:
        """Get the statistics for specified IceWarp service

        Get the statistics for specified IceWarp service

        **Parameters**

        - SType: Service type specification ( see TServiceType )


        **Returns**

        Statistic values for service of type SType

        Endpoint: ``POST /GetServiceStatistics``

        Args:
            stype: Maps to the ``stype`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "stype": stype,
        }
        return self._client.call("GetServiceStatistics", **params)

    def get_services_info_list(self, *, filter: Any | None = None, offset: int | None = None, count: int | None = None) -> Any:
        """Gets the info list of IceWarp services

        Gets the info list of IceWarp services

        **Parameters**

        - Filter: Services list filter ( see TServiceListFilter )
        - Offset: Specifies offset start of returned items
        - Count: Specifies count of returned items


        **Returns**

        Info list of IceWarp services ( see TServicesInfoList )

        Endpoint: ``POST /GetServicesInfoList``

        Args:
            filter: Maps to the ``filter`` request parameter.
            offset: Maps to the ``offset`` request parameter.
            count: Maps to the ``count`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "filter": filter,
            "offset": offset,
            "count": count,
        }
        return self._client.call("GetServicesInfoList", **params)

    def get_statistics_properties(self, *, statisticspropertylist: Any | None = None) -> Any:
        """Gets server statistic properties

        Gets server statistic properties

        **Parameters**

        - StatisticsPropertyList: List of statistics properties you want to retrieve


        **Returns**

        Statistics property list with its rights and values( see TPropertyValueList )

        Endpoint: ``POST /GetStatisticsProperties``

        Args:
            statisticspropertylist: Maps to the ``statisticspropertylist`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "statisticspropertylist": statisticspropertylist,
        }
        return self._client.call("GetStatisticsProperties", **params)

    def get_traffic_charts(self, *, stype: str | None = None, charttype: str | None = None, count: int | None = None, period: str | None = None, ffrom: str | None = None, fto: str | None = None) -> Any:
        """Gets the traffic chart data for specified IceWarp service

        Gets the traffic chart data for specified IceWarp service

        **Parameters**

        - SType: Service type specification ( see TServiceType )
        - ChartType: Type of traffic chart data ( see TServiceChartType )
        - Count: Number of points on x asis
        - Period: Interval Length ( see TServiceChartPeriod )
        - FFrom: Optional interval start (string datetime)
        - FTo: Optional interval end (string datetime)


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /GetTrafficCharts``

        Args:
            stype: Maps to the ``stype`` request parameter.
            charttype: Maps to the ``charttype`` request parameter.
            count: Maps to the ``count`` request parameter.
            period: Maps to the ``period`` request parameter.
            ffrom: Maps to the ``ffrom`` request parameter.
            fto: Maps to the ``fto`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "stype": stype,
            "charttype": charttype,
            "count": count,
            "period": period,
            "ffrom": ffrom,
            "fto": fto,
        }
        return self._client.call("GetTrafficCharts", **params)

    def is_service_running(self, *, service: str | None = None) -> Any:
        """Check if specified IceWarp service is running or not

        Check if specified IceWarp service is running or not

        **Parameters**

        - Service: Service type specification ( see TServiceType )


        **Returns**

        1 if service is running , 0 if the service is stopped

        Endpoint: ``POST /IsServiceRunning``

        Args:
            service: Maps to the ``service`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "service": service,
        }
        return self._client.call("IsServiceRunning", **params)

    def restart_service(self, *, service: str | None = None) -> Any:
        """Restarts the specified IceWarp service

        Restarts the specified IceWarp service

        **Parameters**

        - Service: Service type specification ( see TServiceType )


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /RestartService``

        Args:
            service: Maps to the ``service`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "service": service,
        }
        return self._client.call("RestartService", **params)

    def start_service(self, *, service: str | None = None) -> Any:
        """Starts the specified IceWarp service

        Starts the specified IceWarp service

        **Parameters**

        - Service: Service type specification ( see TServiceType )


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /StartService``

        Args:
            service: Maps to the ``service`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "service": service,
        }
        return self._client.call("StartService", **params)

    def stop_service(self, *, service: str | None = None) -> Any:
        """Stops the specified IceWarp service

        Stops the specified IceWarp service

        **Parameters**

        - Service: Service type specification ( see TServiceType )


        **Returns**

        1 on success, 0 when there is some failiure

        Endpoint: ``POST /StopService``

        Args:
            service: Maps to the ``service`` request parameter.

        Returns:
            The parsed ``result`` value from the response - see ``IceWarpClient.call`` for details.
        """
        params = {
            "service": service,
        }
        return self._client.call("StopService", **params)
